<Project Sdk="Microsoft.NET.Sdk">

  <!-- Not part of the bridge — throwaway console app to generate a
       synthetic-but-structurally-real save file for on-device testing,
       since no real hardware save was available in this session. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="PKHeX.Core" Version="26.8.26" />
  </ItemGroup>

</Project>

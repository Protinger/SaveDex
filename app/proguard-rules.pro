# SaveDex app ProGuard/R8 rules.
# Add project-specific rules here as real code lands; keep default optimizations
# (proguard-android-optimize.txt) enabled rather than disabling shrinking to work
# around a missing rule.
